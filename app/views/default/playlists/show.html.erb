<div class="container">
  <div class="row">
    <div class="col-3">Basic Example</div>
    <div class="col-9">
      <table class="table table-hover table-striped table-sm">
        <tbody id="playlist_items">
          <%= render @playlist.playlist_items.order :position %>
        </tbody>
      </table>
    </div>
  </div>
  <%= render partial: 'default/playlist_items/form', locals: {
    playlist_item: [:default, @playlist, PlaylistItem.new] } %>
</div>
