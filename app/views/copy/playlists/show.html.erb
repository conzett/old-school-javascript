<div class="container">
  <div class="row">
    <div class="col-3">Copy Example</div>
    <div class="col-9">
      <table class="table table-hover table-striped table-sm">
        <tbody id="playlist_items">
          <%= render @playlist.playlist_items.order :position %>
        </tbody>
      </table>
    </div>
  </div>
  <%= render partial: 'copy/playlist_items/form', locals: {
    playlist_item: [:copy, @playlist, PlaylistItem.new] } %>
</div>
